# Host and port for the gRPC server inside Kubernetes
LLM_GRPC_HOST=0.0.0.0
LLM_GRPC_PORT=8080

# ChromaDB or vector store config (adjust if needed)
CHROMA_COLLECTION_NAME=menu_collection
RESTAURANT_ID=restaurant_a

# Directory for embedded menus (if using shared volume or baked into image)
MENU_DIR=/app/tests/menus

# Optional: logging level
LOG_LEVEL=INFO

# Ollama Parameters

OLLAMA_MODEL=llama3:latest
OLLAMA_TEMPERATURE=0.7
OLLAMA_NUM_PREDICT=60
